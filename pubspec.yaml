name: komet
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.5.21+21

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  # Rust networking core (kolibri) — FFI plugin, replaces the Dart transport.
  # Published from the KometTeam/kolibri repo; the native core is compiled at
  # app build time and pulled from that repo by git tag.
  kolibri: ^0.1.4

  # Пробрасывается только ради RustStreamSink: kolibri 0.1.4 не отдаёт isPwa и
  # headerUserAgent через openSessionWithWireLog, а веб-хэндшейку они нужны, так
  # что SessionOptions собираем сами. Версия обязана совпадать с пином kolibri.
  flutter_rust_bridge: 2.12.0

  # Message-encryption core (C over monocypher): legacy passphrase scheme and
  # v2 end-to-end encryption. KometTeam/crypto-core is a git submodule at
  # native/crypto-core, so the version is pinned by the submodule commit;
  # run `git submodule update --init --recursive` after cloning.
  komet_crypto:
    path: native/crypto-core/flutter/komet_crypto

  # Lottie renderer (tlottie, Rust) for animated stickers, animoji and
  # reactions — local FFI plugin; its wrapper crate pins tlottie by git rev and
  # cargokit compiles it at app build time, like kolibri.
  komet_tlottie:
    path: native/komet_tlottie

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  crypto: ^3.0.7
  ffi: ^2.1.0
  logger: ^2.6.2
  device_info_plus: 12.3.0
  flutter_timezone: ^5.0.1
  timezone: ^0.11.0
  file_picker: ^8.0.0
  image_picker: ^1.1.2
  camera: ^0.11.0
  permission_handler: ^11.3.1
  archive: ^4.0.9
  geolocator: ^13.0.0
  photo_manager: ^3.0.0
  image: ^4.3.0
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.4.0+2
  path: ^1.9.1
  m3e_collection: ^0.3.7
  material_symbols_icons: ^4.2906.0
  dynamic_color: ^1.8.1
  shared_preferences: ^2.5.4
  flutter_secure_storage: ^10.3.1
  package_info_plus: ^9.0.1
  mobile_scanner: ^7.2.0
  qr: ^4.0.0
  flutter_contacts: ^1.1.9+2
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  lottie: ^3.3.1
  path_provider: ^2.1.4
  share_plus: ^10.1.4
  open_filex: ^4.5.0
  url_launcher: ^6.3.1
  app_links: ^7.0.0
  video_player: ^2.9.2
  firebase_core: ^4.1.1
  firebase_messaging: ^16.0.2
  flutter_local_notifications: ^22.0.1
  flutter_inappwebview: ^6.1.5
  flutter_webrtc: ^1.5.2
  record: ^6.0.0
  opus_dart: ^3.0.1
  opus_flutter_android: ^3.0.1
  ogg_opus_player: ^0.8.0
  audio_service: ^0.18.19
  audio_session: ^0.2.4
  just_audio: ^0.10.6
  just_audio_media_kit: ^2.1.0
  video_player_media_kit: ^2.0.0
  media_kit_libs_windows_video: ^1.0.11
  media_kit_libs_linux: ^1.2.1
  media_kit_libs_macos_video: ^1.1.4
  sensors_plus: ^7.1.0
  smart_auth: ^3.2.0
  flutter_svg: ^2.0.10
  fjs: ^3.3.0
  cryptography: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  path_provider_platform_interface: ^2.1.0
  plugin_platform_interface: ^2.1.0

flutter_launcher_icons:
  image_path: "assets/komet_icon.png"
  android: "ic_launcher"
  ios: true
  remove_alpha_ios: true
  background_color_ios: "#000000"
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "assets/komet.png"
  windows:
    generate: true
    image_path: "assets/komet_icon.png"
    icon_size: 256
  macos:
    generate: true
    image_path: "assets/komet_icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  shaders:
    - shaders/liquid_glass.frag
    - shaders/mesh_gradient.frag

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/komet.png
    - assets/komet_icon.png
    - assets/meteor_icon.png
    - assets/emoji_keywords.json
    - assets/lottie/
    - assets/wallpapers/patterns/
    - assets/debug/
    - assets/plugins/nekos/
    - assets/plugins/info/
    - assets/plugins/weather/

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter.ttf
    - family: Outfit
      fonts:
        - asset: assets/fonts/Outfit.ttf
    - family: Unbounded
      fonts:
        - asset: assets/fonts/Unbounded.ttf

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
